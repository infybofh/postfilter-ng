# Administration commands

`postfilterctl` is the only installed administration program.  It performs configuration checks, offline article explanation, SQLite queries/backups/purges, report generation and encrypted-value decoding.
