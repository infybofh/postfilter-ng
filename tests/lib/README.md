# Test helper library

`TestPostfilter.pm` builds isolated in-memory-style configurations and realistic
article fixtures shared by the release tests.  It is never installed into INN's
runtime library path and contains no production policy.
