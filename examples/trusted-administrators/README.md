# Trusted administrators

Profiles are fully explicit.  `match_mode = "all"` requires every configured
condition; `any` accepts any one.  Keep structural checks unless `full_bypass`
is consciously enabled.

## Example fragment

Copy and adapt `example.toml`; do not replace the main configuration blindly.
