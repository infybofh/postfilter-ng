# Detailed ban rules

The first rule demonstrates parsed mailbox matching and spelling variants.
Commented expressions show incorrect dot escaping and missing anchors alongside
corrected forms.

## Example fragment

Copy the required `[[ban_rule]]` tables from `example.toml` into a late
`conf.d/` file and validate the complete configuration.
