# Authenticated reader

Named nnrpd users are limited by account ID.  The article's From/Sender headers
are not assumed to be the authentication identity and remain unchanged.

## Example fragment

Copy and adapt `example.toml`; do not replace the main configuration blindly.
